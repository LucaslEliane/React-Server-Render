const userInfoReducer = function (state = {}, action) {
  // switch(action.type) {
  //   case "USE_ACTION":
  //     return Object.assign({}, state)
  //   default:
  //     return state
  // }
  return state
}

export default userInfoReducer